<Page x:Class="esoft.Nmobles.RemoteNmobles"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:esoft.Nmobles"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RemoteNmobles">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Click="ScrolPage" x:Name="ButtonGoApartaments" Content="Квартиры" Background="{StaticResource DarkGrey1}" Margin="15,0,15,0"/>
            <Button Click="ScrolPage" x:Name="ButtonGoHouse" Content="Дома" Grid.Column="1" Background="{StaticResource DarkGrey1}" Margin="15,0,15,0"/>
            <Button Click="ScrolPage" x:Name="ButtonGoLand" Content="Земля" Grid.Column="2" Background="{StaticResource DarkGrey1}" Margin="15,0,15,0"/>
        </Grid>

        <Grid Grid.Row="1">
            <Frame x:Name="FrameNmobles"/>
        </Grid>

    </Grid>
</Page>
